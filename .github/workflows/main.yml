on: push
name: Deploy website on push
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v2.3.2
    
    - name: Sync files
        # uses: ./
        # with:
        #   server: ftp.samkirkland.com
        #   username: test@samkirkland.com
        #   password: ${{ secrets.ftp_password }}
    # - name: Sync files
    #   uses: Shary81/testing@v1.0
    #   with:
    #     server: ftp.warmprospect.com
    #     username: warmprospect@app.warmprospect.com
    #     password: K&davv?@@8U7$